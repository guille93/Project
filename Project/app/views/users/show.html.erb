<img class="fondo" src="<%= @user.fondo %>">
<div class="row">
	<div class="col-md-4 col-sm-4 user">
	<img id="userimg" src="<%= @user.avatar %>">
	<p>
	Usuario: <%= @user.first_name %> <%= @user.last_name %> 
	</p>
	<p>
	Email: <%= @user.email %> 
	</p>
	<p>
	Fecha de Registro: <%= @user.created_at %> 
	</p>
</div>


	<div class="col-md-3 col-sm-3" id="show">
		<div id="titles">
		 Tus decisiones
		</div>
		<hr>
	<% @decisions.each do |decision| %>
		<li id="lista"><%= decision.name_1 %> Vs <%= decision.name_2 %> <%= link_to 'Mostrar', decision%> </li>
	<% end %>


	</div>

	<div class="col-md-3 col-sm-3" id="show">
		<div id="titles">
		Tus Últimas Decisiones 
	</div>
		<hr>
<% @decisions.limit(2).each do |decision| %>
<li id="lista"><%= decision.name_1 %> Vs <%= decision.name_2 %> <%= link_to 'Mostrar', decision%> </li>
<% end %>
</div>
</div>